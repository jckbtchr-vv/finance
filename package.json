{
  "name": "finance-calculator",
  "version": "1.0.0",
  "description": "Mortgage mechanics calculator",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist/mortgage dist/lease && cp index.html dist/ && cp -r mortgage/* dist/mortgage/ && cp -r lease/* dist/lease/"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}

